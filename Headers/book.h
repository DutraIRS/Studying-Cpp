using std::string;

typedef struct Book
{
    string strTitle;
    string strAuthor;
    int iPublishDate;        
} Book; // in order not to have to use struct Book

void organizedFunction(int iValue);